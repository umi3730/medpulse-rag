# 环境变量管理（可选，支持 .env 文件）
python-dotenv>=1.0.0

# 数据爬取
requests>=2.28.0
lxml>=4.9.0

# LLM 集成（LangChain 核心）
langchain-core>=0.3.0
langgraph>=0.2.0
qdrant-client>=1.12.0

# LLM 提供商（按需安装，至少选一个）
langchain-ollama>=0.3.0         # 本地 Ollama（默认）
# langchain-openai>=0.3.0       # OpenAI / DeepSeek 等兼容 API → pip install langchain-openai
# langchain-anthropic>=0.3.0    # Anthropic Claude → pip install langchain-anthropic

# 知识图谱构建 & 问答
py2neo>=2021.2.3
neo4j>=6.2.0

# 实体归一化（模糊匹配）
rapidfuzz>=3.0.0

# 问句分类降级（AC自动机，LLM 不可用时使用）
pyahocorasick>=2.0.0

# Web API 服务
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
